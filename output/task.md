# AI Daily Lab — 2026-03-21

## Task
Develop a machine learning pipeline to predict the likelihood of a loan defaulting, based on applicant demographics, loan terms, and early repayment behavior.

## Focus
Binary Classification (Loan Default Prediction)

## Dataset
{'name': 'Synthetic Loan Data', 'dataframes': [{'name': 'applicants_df', 'rows': '500-700', 'columns': {'applicant_id': 'unique integers', 'application_date': 'random dates over the last 5 years', 'credit_score': 'random integers 300-850', 'income': 'random integers 30000-200000', 'loan_purpose': "e.g., 'Home', 'Car', 'Education', 'Debt_Consolidation', 'Other'", 'employment_status': "e.g., 'Employed', 'Self-Employed', 'Unemployed'"}}, {'name': 'loans_df', 'rows': '500-700 (1:1 with applicants for simplicity)', 'columns': {'loan_id': 'unique integers', 'applicant_id': 'randomly sampled from `applicants_df` IDs', 'loan_amount': 'random floats 5000-50000', 'loan_term_months': 'random integers 12-60', 'interest_rate': 'random floats 0.05-0.25', 'disbursement_date': 'random dates occurring *after* their respective `application_date`'}}, {'name': 'repayments_df', 'rows': '5000-8000', 'columns': {'repayment_id': 'unique integers', 'loan_id': 'randomly sampled from `loans_df` IDs', 'repayment_date': 'random dates occurring *after* their respective `disbursement_date` and monthly up to `loan_term_months`', 'expected_amount_per_month': 'calculated based on `loan_amount`, `loan_term_months`, `interest_rate`', 'amount_paid': 'random floats, mostly equal to `expected_amount_per_month`, but sometimes less', 'is_late': 'binary, 1 if `amount_paid` is significantly less than `expected_amount_per_month`'}}], 'patterns': ['Ensure `disbursement_date` is after `application_date` and `repayment_date` is after `disbursement_date`.', 'Calculate `expected_amount_per_month` (e.g., simple approximation: `(loan_amount / loan_term_months) * (1 + interest_rate / 12)` for synthetic purposes).', "Bias `interest_rate` such that lower `credit_score` applicants or 'Unemployed' individuals have higher rates.", "Bias `is_late` events and `amount_paid < expected_amount_per_month` such that they are more frequent for loans associated with lower `credit_score`, 'Unemployed' status, or 'Debt_Consolidation' purpose.", "Ensure some loans eventually accumulate enough underpayments or late payments to be considered 'bad loans'."]}

## Hint
For SQL feature engineering, filter `repayments` for the 'early behavior window' using `r.repayment_date BETWEEN l.disbursement_date AND DATE(l.disbursement_date, '+90 days')`. For the `is_bad_loan` target, you'll need to aggregate total `amount_paid` for each loan across *all* its repayment history and compare it to the total expected payment (loan_amount plus total interest over the term). Use `julianday()` for date differences in SQL.
